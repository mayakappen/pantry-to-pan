class Pantry {
    constructor(user) {
        this.ingredientsOwned = user.pantry;
        this.ingredientsToBuy = [];
    }

    checkForIngredients(recipe, repo) {
        // recipe.getIngredients(repo);
        
    }
}

export default Pantry;
